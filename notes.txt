Integer Division produces floating results
String Conversion:
        int(),float()

#comment

integer is implicitly converted to a float when in expression with float

None : like null

== : equal in value
is : equal in type and value

open() returns a "file handle" - a variable used to perform operations on the file.

list:
    to make a list: stuff = [] or list()

split(): Removes specified characters(default is white space) from String and creates a List from it


dictionary:
  stuff = dict()
  dict() same as { }
  .get(key, default) -- get the value of the key; if not in dict() then set to default value
  list(a dict) -- get all keys
  .kets() -- get all keys
  .values() -- get all values
  .items() -- get a list of both key value

  we can:
    Two iteration variables:
      for var1,var2 in list.items():
        print(var1,var2)

        where var1 is key, var2 is value, and .items() returns a k,v pair
